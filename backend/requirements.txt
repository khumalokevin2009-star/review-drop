# Orvelle backend dependencies — see CLAUDE.md Section 5 (tech stack).

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Database / ORM / migrations ---
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# --- Validation / settings ---
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0

# --- Auth & security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3


# --- Website rendering / screenshots / storage ---
playwright==1.60.0  # 1.44 predates Debian 13 (trixie); --with-deps fails there
boto3==1.34.131            # Cloudflare R2 (S3-compatible)
httpx==0.27.0             # proxy fetching + test client
beautifulsoup4==4.12.3    # proxy HTML URL rewriting
html5lib==1.1             # browser-accurate HTML parsing for the proxy

# --- Billing / email / monitoring ---
stripe==9.12.0
resend==2.1.0
sentry-sdk[fastapi]==2.5.1

# --- Rate limiting ---
slowapi==0.1.9

# --- Environment ---
python-dotenv==1.0.1

# --- Dev / test / lint ---
pytest==8.2.2
pytest-asyncio==0.23.7
ruff==0.4.9
black==24.4.2
